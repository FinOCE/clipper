import { Channel, Guild, Message, User } from "discord.js"

/**
 * Generate a screenshot of a message and then DM it to the specified user.
 * @param user The user to send the screenshot to
 * @param message The message to take a screenshot of
 */
export default async function bookmark(user: User, guild: Guild | null, channel: Channel | null, message: Message) {
  console.log("TODO: Screenshot message now!")
}
